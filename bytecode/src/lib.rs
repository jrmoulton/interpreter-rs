#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum OpCode {
    Const(usize),
    JumpNotTruthy(usize),
    Jump(usize),
    Add,
    Sub,
    Mul,
    Div,
    Neg,
    Positive,
    Equal,
    NotEqual,
    GreaterThan,
    LessThan,
    Bang,
    Pop,
    Print,
    NoOp,
}
